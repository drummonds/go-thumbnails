# Roadmap

## Planned

- Single-page PDF render optimisation (avoid rendering all pages when only one is needed)
- Multi-page TIFF support (pending golang.org/x/image/tiff DecodeAll or alternative decoder)
- WebP output format support
